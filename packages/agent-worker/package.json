{
  "name": "agent-worker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "dotenv -e .dev.vars -- mastra dev",
    "dev-wrangler": "pnpm run build && cp .dev.vars .mastra/output && wrangler dev --remote --config .mastra/output/wrangler.json",
    "build": "mastra build",
    "postbuild": "node ./scripts/postbuild.js",
    "deploy": "        wrangler --config .mastra/output/wrangler.json deploy",
    "deploy-preview": "wrangler --config .mastra/output/wrangler.json versions upload --tag $(git rev-parse --short HEAD) --preview-alias ${WORKERS_CI_BRANCH:-$(git rev-parse --abbrev-ref HEAD)}"
  },
  "dependencies": {
    "@ai-sdk/openai": "2.0.88",
    "@line/bot-sdk": "10.5.0",
    "@mastra/cloudflare-d1": "1.0.0-beta.8",
    "@mastra/core": "1.0.0-beta.18",
    "@mastra/deployer-cloudflare": "1.0.0-beta.1",
    "@mastra/memory": "1.0.0-beta.9",
    "dotenv-cli": "8.0.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "mastra": "1.0.0-beta.12"
  }
}
